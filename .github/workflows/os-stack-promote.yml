name: Promote App Harness OS stack node

on:
  workflow_dispatch:
    inputs:
      pull_request: { required: true, type: string }
      stack_id: { required: true, type: string }
      generation: { required: true, type: string }
      issue_number: { required: true, type: string }
      parent_branch: { required: true, type: string }
      head_sha: { required: true, type: string }
      room: { required: true, type: string }
      workflow_id: { required: true, type: string }
      ci_profile: { required: true, type: string }

permissions:
  contents: write
  pull-requests: write
  issues: write

concurrency:
  group: app-harness-stack-${{ inputs.stack_id }}-generation-${{ inputs.generation }}
  cancel-in-progress: false

jobs:
  promote-root:
    runs-on: ubuntu-latest
    env:
      GH_REPO: ${{ github.repository }}
      GH_TOKEN: ${{ secrets.AUTONOMY_GITHUB_TOKEN }}
      PR: ${{ inputs.pull_request }}
      STACK_ID: ${{ inputs.stack_id }}
      GENERATION: ${{ inputs.generation }}
      ISSUE: ${{ inputs.issue_number }}
      EXPECTED_PARENT: ${{ inputs.parent_branch }}
      EXPECTED_HEAD: ${{ inputs.head_sha }}
      AUTONOMY_REQUEST_ID: ${{ inputs.workflow_id }}
      AUTONOMY_ROOM: ${{ inputs.room }}
      AUTONOMY_CALLBACK_URL: ${{ secrets.AUTONOMY_CALLBACK_URL }}
      AUTONOMY_GITHUB_TOKEN: ${{ secrets.AUTONOMY_GITHUB_TOKEN }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CI_PROFILE: ${{ inputs.ci_profile }}
    steps:
      # This must precede provenance and the failure callback: both use
      # repository files, and provenance must be independent of a runner's
      # implicit checkout state.
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Verify immutable root stack provenance
        run: |
          # shellcheck disable=SC2016
          node -e '
            const fs = require("node:fs");
            const { execFileSync } = require("node:child_process");
            const pr = JSON.parse(execFileSync("gh", ["api", `repos/${process.env.GH_REPO}/pulls/${process.env.PR}`], { encoding: "utf8" }));
            const expected = pr.base.ref === process.env.EXPECTED_PARENT && pr.head.sha === process.env.EXPECTED_HEAD && /^app-harness-os\/\d+\/g\d+$/.test(pr.head.ref);
            const alreadyMerged = expected && pr.state === "closed" && Boolean(pr.merged_at) && typeof pr.merge_commit_sha === "string";
            if (!(expected && (pr.state === "open" || alreadyMerged))) throw new Error("stale or invalid stack provenance");
            if (alreadyMerged) {
              fs.appendFileSync(process.env.GITHUB_ENV, "ALREADY_MERGED=true\n");
              fs.appendFileSync(process.env.GITHUB_ENV, `MERGED_SHA=${pr.merge_commit_sha}\n`);
            }
          '
      - name: Require the supported effective CI profile
        run: test "$CI_PROFILE" = visual
      - name: Wait for the immutable stack-node validation status
        run: |
          attempt=1
          while [ "$attempt" -le 36 ]; do
            # GitHub returns status history newest-first. Use the newest
            # matching immutable validation result, never a stale earlier run.
            result="$(gh api "repos/${GH_REPO}/commits/${EXPECTED_HEAD}/status" --jq '.statuses[] | select(.context == "app-harness-os/validate-node") | .state' | head -n 1)"
            if [ "$result" = success ]; then exit 0; fi
            if [ -n "$result" ] && [ "$result" != null ]; then echo "validate-node concluded: $result"; exit 1; fi
            sleep 5
            attempt=$((attempt + 1))
          done
          echo "validate-node did not register for the immutable candidate head"; exit 1
      - name: Check out the immutable stack tip
        run: |
          if [ "$ALREADY_MERGED" = true ]; then
            git fetch origin main
            git checkout --detach "$MERGED_SHA"
          else
            git fetch origin "pull/${PR}/merge:refs/remotes/origin/app-harness-os-merge-${PR}"
            git checkout --detach "origin/app-harness-os-merge-${PR}"
          fi
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Current stack-tip integration gate
        run: |
          test "$(git rev-parse HEAD)" != ""
          npm run cf-typegen
          npx tsc --noEmit
          npm run test:stack
          npm run test:native-git-preflight
          npm run test:os-provider-bridge
          git diff --check
          node scripts/check-visual-candidate.mjs
      - name: Merge the validated current generation
        if: env.ALREADY_MERGED != 'true'
        run: |
          cat > /tmp/app-harness-promoting.md <<'EOF'
          Stack promotion started after the current node and stack-tip gates passed.
          EOF
          gh issue comment "$ISSUE" --body-file /tmp/app-harness-promoting.md
          gh pr merge "$PR" --squash --delete-branch
      - name: Fetch and verify merged main before deployment
        run: |
          git fetch origin main
          git checkout --detach origin/main
          merge_sha="${MERGED_SHA:-$(gh pr view "$PR" --json mergeCommit --jq '.mergeCommit.oid')}"
          test -n "$merge_sha"
          if [ "$ALREADY_MERGED" = true ]; then
            git merge-base --is-ancestor "$merge_sha" HEAD
          else
            test "$(git rev-parse HEAD)" = "$merge_sha"
          fi
      - name: Deploy verified merged main
        if: env.ALREADY_MERGED != 'true'
        run: npx wrangler deploy
      - name: Mark the public issue completed
        run: |
          gh label create 'status:completed' --color 1f883d --description 'App Harness completed' --force
          gh issue edit "$ISSUE" --add-label 'status:completed'
      - name: Publish durable completion callback
        run: |
          if [ "$ALREADY_MERGED" = true ]; then
            node scripts/autonomy-callback.mjs completed "Cloudflare OS stack ${STACK_ID} generation ${GENERATION} reconciled a previously verified merge and deployment." "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          else
            node scripts/autonomy-callback.mjs completed "Cloudflare OS stack ${STACK_ID} generation ${GENERATION} validated, merged, and deployed." "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          fi
      - name: Publish safe failure callback
        if: failure()
        run: node scripts/autonomy-callback.mjs requires_review "Cloudflare OS stack promotion did not pass immutable provenance, CI, merge, or deployment gates. Review the linked GitHub Actions run; no success is claimed." "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
