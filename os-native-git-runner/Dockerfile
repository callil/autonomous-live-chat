FROM docker.io/cloudflare/sandbox:0.12.4

# The upstream Sandbox image supplies Linux, Git, Node, and the execution API.
# No repository, source, model credential, or GitHub credential is baked in.
EXPOSE 8080
