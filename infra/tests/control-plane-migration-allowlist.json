{
  "version": 1,
  "purpose": "Temporary, explicit inventory of demo-worker responsibilities that must move behind the Cloudflare OS operator. Each entry is a current violation of the target boundary. Delete an entry in the same change that removes the matching implementation. The file must reach an empty entries array before the simplified-control-plane goal can be completed.",
  "entries": [
    {
      "id": "demo-coordinator-job-prefix",
      "file": "apps/demo/src/index.ts",
      "expectedCount": 1,
      "removal": "Remove the one-time legacy storage deletion after the ledger-only migration has run in production. It performs no orchestration."
    },
    {
      "id": "demo-coordinator-outbox-prefix",
      "file": "apps/demo/src/index.ts",
      "expectedCount": 1,
      "removal": "Remove the one-time legacy storage deletion after the ledger-only migration has run in production. It performs no orchestration."
    }
  ]
}
